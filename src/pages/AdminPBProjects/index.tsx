import React from 'react';
import S, { classes } from './styles';

const AdminPBProjects = (): JSX.Element => {

  return (
    <S.AdminPBProjects className={classes.root}>
    </S.AdminPBProjects>
  );
};

export default AdminPBProjects;
